version: '3.8'

services:
  wai-bot:
    build: .
    restart: always
    environment:
      # Application Config
      - PORT=3001
      - NODE_ENV=production

      # Database Persistence (Internal Path)
      - DB_PATH=/app/data/database.sqlite

      # Secrets (To be filled in Coolify UI)
      - GOOGLE_CALENDAR_ID
      - GOOGLE_JSON_CREDENTIALS
      - GEMINI_API_KEY
      - WHATSAPP_PHONE_NUMBER_ID
      - WHATSAPP_TOKEN
      - VERIFY_TOKEN

    volumes:
      - wai_data:/app/data
      - wai_uploads:/app/uploads

    ports:
      - "3001:3001"

volumes:
  wai_data:
  wai_uploads:
